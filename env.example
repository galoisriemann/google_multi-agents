# Environment Variables for Workflow Google ADK (OPTIONAL)
# These are fallback options if not configured in YAML files

# Google API Key for Gemini models (optional - can be set in YAML instead)
# Get your API key from: https://ai.google.dev/
# GOOGLE_API_KEY=your_google_api_key_here

# Optional: Set log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Optional: Set custom model (defaults to gemini-1.5-flash)
# GEMINI_MODEL=gemini-1.5-pro 